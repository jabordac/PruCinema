<MudThemeProvider Theme="MiTema" />
<MudSnackbarProvider/>
<MudDialogProvider/>

<CascadingAuthenticationState>
    <Router AppAssembly="@typeof(Program).Assembly" PreferExactMatches="@true">
        <Found Context="routeData">
            <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)">
                <Authorizing>
                    <p>Autorizando...</p>
                </Authorizing>
                <NotAuthorized>
                    @if (context.User.Identity is { IsAuthenticated: false })
                    {
                        <RedirectToLogin></RedirectToLogin>
                    }
                    else
                    {
                        <p>No tiene autorización para acceder a esta ubicación.</p>
                    }
                </NotAuthorized>
            </AuthorizeRouteView>
        </Found>
        <NotFound>
            <LayoutView Layout="@typeof(MainLayout)">
                <p>No se ha encontrado el recurso solicitado.</p>
            </LayoutView>
        </NotFound>
    </Router>
</CascadingAuthenticationState>

@code {
    MudTheme MiTema = new MudTheme()
    {
        Palette = new Palette()
        {
            DrawerBackground = "#F5F5F5",
        },

        LayoutProperties = new LayoutProperties()
        {
            DrawerMiniWidthLeft = "70px",
            DrawerWidthLeft = "250px",
        },

        Typography = new Typography()
        {
            //Default = new Default()
            //{
            //    FontFamily = new[] { "Montserrat", "Roboto", "Helvetica", "Arial", "sans-serif" },
            //},
        },
    };
}